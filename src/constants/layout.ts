/** Placement of the receiver name labels over the 3D board. */

/** Above this viewport size the labels get more breathing room. */
export const DESKTOP_MIN_WIDTH = 900;
export const DESKTOP_MIN_HEIGHT = 600;

/** Gap between a receiver and its name label. */
export const LABEL_GAP_DESKTOP = 12;
export const LABEL_GAP_MOBILE = 6;

/** Desktop pushes the opponent label down past the wider board frame. */
export const OPPONENT_LABEL_DESKTOP_OFFSET = 50;
