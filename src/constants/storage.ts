/** Browser-storage keys. Changing one silently orphans saved data. */

export const PLAYER_NAME_KEY = "twivvy-player-name";

/** Longest name the input accepts, mirrored by the template's maxlength. */
export const MAX_PLAYER_NAME_LENGTH = 24;
